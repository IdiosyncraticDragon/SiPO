# How to run ticket pruning 

access to data and model
```bash
ln -s ../data data
ln -s ../model deepMpdels
```

ticket of LM
```bash
cd LMSWPO
```

ticket of NMT
```bash
cd NMTSWPO
```

